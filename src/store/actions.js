export function loadInfo() {
  return new Promise(( resolve ) => {
    setTimeout(() => resolve(), 0 );
  })
}

export function loadData() {
  return new Promise(( resolve ) => {
    setTimeout(() => resolve(), 0 );
  })
}
